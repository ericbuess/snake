// Stub for a simple chess AI integration

export function computeAIMove(board) {
  // A place-holder AI that simply selects a random legal move.
  // TODO: Replace with a minimax algorithm or integration with Stockfish WASM.

  // For this stub, assume we return a move object { source: "e2", destination: "e4" }
  // In a complete version, enumerate all moves and pick one.
  return { source: "e2", destination: "e4" };
}
